import { motion } from "framer-motion";
import { CalendarDays, CarFront, CircleX, MapPin } from "lucide-react";
import { useLocation } from "wouter";
import Footer from "@/components/Footer";
import Navbar from "@/components/Navbar";
import { useBookings } from "@/lib/useBookings";

export default function MyDrivePage() {
  const [, setLocation] = useLocation();
  const { bookings, cancelBooking } = useBookings();
  return <div className="min-h-screen bg-[#f7f8f6] text-[#0f1e2e]"><Navbar /><main><section className="bg-[#eaf0f3] py-[4.5rem] sm:py-20"><div className="container"><p className="eyebrow"><span /> My Drive</p><div className="mt-5 grid gap-7 lg:grid-cols-[0.85fr_1.15fr] lg:items-end"><h1 className="max-w-2xl font-editorial text-5xl leading-[0.92] tracking-[-0.06em] sm:text-7xl">Your browser-local request history.</h1><p className="max-w-xl justify-self-end text-sm leading-7 text-slate-600">Review request references created in this browser. This screen is a front-end demonstration and does not represent confirmed reservations.</p></div></div></section><section className="py-[4.5rem] sm:py-20"><div className="container">{bookings.length ? <div className="grid gap-5 lg:grid-cols-2">{bookings.map((booking, index) => <motion.article key={booking.id} initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.05 }} className="overflow-hidden rounded-[24px] border border-slate-200 bg-white shadow-[0_10px_30px_rgba(15,30,46,0.05)]"><div className="grid sm:grid-cols-[180px_1fr]"><img src={booking.carImage} alt="" className="h-full min-h-44 w-full object-cover" /><div className="p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="fleet-kicker text-slate-400">{booking.id}</p><h2 className="mt-2 font-editorial text-3xl tracking-[-0.05em]">{booking.carName}</h2></div><span className={`rounded-full px-3 py-1.5 text-[11px] font-extrabold uppercase tracking-[0.1em] ${booking.status === "Cancelled" ? "bg-slate-100 text-slate-500" : "bg-green-50 text-[#16a34a]"}`}>{booking.status}</span></div><div className="mt-5 grid gap-3 text-sm font-semibold text-slate-600"><p className="flex items-center gap-2"><CalendarDays className="h-4 w-4 text-[#d97706]" /> {booking.pickupDate} → {booking.returnDate}</p><p className="flex items-center gap-2"><MapPin className="h-4 w-4 text-[#d97706]" /> {booking.pickupLocation ?? "Collection point to be confirmed"}</p></div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><p className="font-editorial text-3xl text-[#d97706]">${booking.total}</p>{booking.status !== "Cancelled" && <button onClick={() => cancelBooking(booking.id)} className="flex items-center gap-1.5 text-xs font-extrabold text-slate-500 transition hover:text-red-600"><CircleX className="h-3.5 w-3.5" /> Cancel request</button>}</div></div></div></motion.article>)}</div> : <div className="flex min-h-80 flex-col items-center justify-center rounded-[26px] border border-dashed border-slate-300 bg-white px-6 text-center"><span className="flex h-14 w-14 items-center justify-center rounded-full bg-[#eaf0f3]"><CarFront className="h-6 w-6" /></span><h2 className="mt-5 font-editorial text-4xl">No requests in this browser yet.</h2><p className="mt-3 max-w-md text-sm leading-7 text-slate-500">Choose a vehicle, complete the three-step request flow, and it will appear here for quick reference.</p><button onClick={() => setLocation("/#fleet")} className="mt-7 rounded-xl bg-[#0f1e2e] px-5 py-3.5 text-sm font-extrabold text-white transition hover:bg-[#d97706]">Find your first car</button></div>}</div></section></main><Footer /></div>;
}
