# Compact Filter Rail Verification

At desktop width, the rebuilt filter rail measures **300px × 620px** with sixteen **30px** option rows, two-column groups, a reduced heading area, and a compact reset action. It is substantially shorter than the prior single-column rail while each category and label remains visible.

At **375px**, the rail measures **343px** wide with no document overflow. The sixteen compact options remain visible in a stacked layout with **29px** rows, preserving the existing touch and keyboard controls while reducing the vertical footprint.
